# 小手机项目规则与当前目标

## 当前唯一目标

现在只做一件事：

做一个“小手机快捷指令/后台唤醒机制”：

当用户长时间没有打开小手机网页时，小手机能自动被唤起或自动进入一次检测流程，触发角色消息/提醒，而不是必须用户手动打开。

重点目标不是抖音，不是 GitHub Pages，不是重新部署旧东西，而是：

确认“小手机久未打开后，是否能自动触发 phone_idle / 角色消息”。

---

## 已排除：不要再重复

以下方案已经试过或方向不对，没有新依据不要再让我重复：

1. 不要再让我反复 Push GitHub
原因：本地 HEAD 和 origin/main 已确认一致，Push 不是当前问题。

2. 不要再让我重新建 Supabase 项目
原因：旧项目可用，数据库和 Edge Function 已经存在。

3. 不要再让我重复测试同一个 webapp://...#event=phone_idle 链接
原因：昨天已经试过，结果不稳定。有时成功，有时没消息。没有新变量不要重测。

3a. 不要再让我重复测试 webapp://...?event=phone_idle 链接
原因：2026-07-09 已实测，能打开主屏小手机，但调试面板收到时间仍停留在上一次模拟，说明 query 事件也没有传进主屏 webapp。

4. 不要继续做抖音方案
原因：方向不对，不做。

5. 不要走“靠通知让我手动点”的方案
原因：我要的是自动触发，不是手动点通知。

6. 不要继续绕回“等待 + 打开 URL”这种旧流程
原因：已经试过，结果不稳定。

---

## 已验证但不稳定

1. 自动跳转：成功过，但不稳定。
2. 跳转后角色消息：成功过一次，但多次没有消息。
3. 小手机本体已有诊断能力，但信息还不够判断卡在哪一步。

## 本次新增验证

1. v379 本地模拟已验证：小手机本体可以收到模拟 phone_idle，进入处理逻辑，选中角色，打开聊天页。
2. v379 本地错误路径已验证：聊天 API 未配置时，调试面板会显示“API配置/权限”，不会误判成快捷指令问题。
3. v379 本地成功路径已验证：AI 返回正常内容时，调试面板会显示“已生成角色消息”，聊天里会出现角色消息。
4. v379 手机实测已验证：在设置页点击“模拟久未打开超过1分钟”后，角色成功发消息。说明小手机本体收到 phone_idle 后的内部链路已经打通。
5. v379 手机实测已验证：快捷指令用 webapp://...#event=phone_idle 可以打开主屏小手机，但调试面板“收到 phone_idle”仍停留在上一次模拟时间，说明 hash 事件没有传进主屏 webapp。
6. v379 手机实测已验证：快捷指令用 webapp://...?event=phone_idle 也可以打开主屏小手机，但调试面板“收到 phone_idle”仍停留在上一次模拟时间，说明 query 事件同样没有传进主屏 webapp。
7. v380 本地浏览器已验证：打开 phone-idle-local.html 后，页面能写入 localStorage 待处理 phone_idle，随后主页面能消费该事件并进入处理逻辑。
8. v380 手机实测已排除：快捷指令打开 webapp://.../phone-idle-local.html 后，主屏小手机能打开，但调试面板“收到 phone_idle”时间没有变化。说明 iPhone 主屏 webapp 没有稳定执行这个本地桥页面，不能继续重复测这一条。
9. v380 手机实测云端收件箱第一次失败：12:48 运行“先 POST phone_external_events，再打开主屏小手机”后，调试面板“收到 phone_idle”没有变化；随后电脑查询 Supabase，目标 yb_pelf2zn3tlgool48abeeiemr5d1h2c 没有 12:48 新记录，说明问题在快捷指令 POST 没成功，不是小手机内部处理失败。
10. v380 手机实测：删除请求体里的 payload，只保留 target/event 后再次运行，调试面板仍未变化；电脑查询 Supabase 仍没有新记录。说明当前重点不是小手机取事件，而是快捷指令的“获取 URL 内容”没有成功写入云端或没有按预期执行。
11. v380 电脑诊断已验证：电脑向同一 target POST phone_idle 到 phone_external_events 成功；随后用户手动打开小手机，调试面板“收到 phone_idle”时间更新。说明小手机云端收件箱轮询正常，剩余问题只在 iPhone 快捷指令 POST 写入方式。
12. v380 手机快捷指令诊断：添加“显示结果”后抓到错误 No API key found，说明 iPhone 快捷指令没有把 apikey 头部成功发出；把 apikey 改放到 URL 参数后，错误变为 PGRST204 Could not find the 'event ' column，说明 API key 已生效，剩余问题是请求体字段名 event 后面多了空格。
13. v380 手机快捷指令诊断：删除重建 event 字段后，错误变为 PGRST204 Could not find the 'target ' column，说明 event 字段已修好，target 字段名后面也有隐藏空格，需要同样删除重建 target 字段。
14. v380 手机实测成功：清理请求体字段名隐藏空格后，快捷指令成功 POST 到 phone_external_events；电脑确认云端出现 13:06 新记录；用户点“完成”继续打开主屏小手机后，设置页“久未打开调试”的“收到 phone_idle”时间成功更新。当前可用链路为：快捷指令先 POST 云端事件，再打开主屏小手机，小手机轮询云端收件箱处理 phone_idle。
15. v380 手机实测成功：删除临时“显示结果”后再次运行快捷指令，无弹窗，直接打开主屏小手机；小手机成功收到 phone_idle，角色自动跳到微信并发送消息。当前核心链路已可用。
16. v380 手机自动化实测成功：用“打开抖音时”自动化运行成功快捷指令，可以自动跳到主屏小手机并触发角色微信消息；在自动化内加入“等待 60 秒”后，打开抖音约 1 分钟后自动跳小手机并发消息成功。当前待解决问题：多次打开触发 App 可能启动多个并行倒计时，需要加计时锁防重复。
17. 旧抖音方案可复用的部分：本机文件 lock 防重复计时。流程是检查“我的 iPhone/douyin_lock.txt”，存在且有内容就停止；不存在就写入 lock 文件，等待测试时间，结束后打开小手机。新方案只复用 lock 思路，lock 文件应改名为 phone_idle_lock.txt，不再使用 douyin_timeout 链接。
18. v380 手机计时锁调试：phone_idle_lock.txt 已能写入，文件显示 4 字节，说明落锁成功。手动连续点快捷指令播放时，第二次无法点击，是因为第一轮快捷指令仍在“等待 60 秒”执行中；这个现象不能单独证明 lock 防重复，需要用自动化连续触发测试。
19. v380 手机计时锁调试：等待结束后已成功跳小手机一次，但 phone_idle_lock 文件仍保留 4 字节 lock，导致第二次不会再跳。说明落锁成功、阻止重复成功，但清锁失败；下一步只修“删除 phone_idle_lock”这一步。
20. v380 手机自动化闭环成功：修正清锁步骤为“等待后重新从路径获取 phone_idle_lock.txt，再删除该文件，立即删除开启”。实测自动化成功触发，等待 60 秒后自动跳小手机并发消息，结束后 phone_idle_lock 文件不存在。当前 1 分钟测试版闭环已通过。
21. v380 多 App 自动化实测暴露问题：选择 3 个 App 后快速依次打开，每个 App 似乎都会启动 60 秒计时，之后反复出现删除 phone_idle_lock 文件的动作/提示。说明“删除文件清锁 + 多 App 快速触发”仍可能产生多个并行等待队列；下一步不要继续扩大 App 数量，应改成更稳的锁方案，避免删除确认和并发清锁。
22. v380 重要排除：在没有改动自动化结构之前，不要再让用户重复测试“同一个自动化里多选多个 App”。该方案已验证会产生多个并行倒计时/清锁提示，没有新变量前禁止重测。
23. v380 多自动化实测也失败：拆成多个 App 单独自动化、都运行同一个快捷指令后，仍会出现重复倒计时和重复删除文件提示。说明问题不只在“同一个自动化多选 App”，而是删除文件清锁方案本身不适合多触发并发场景。下一步必须去掉“删除文件”，改成固定 lock 文件存在、通过内容 lock/空 来判断。
24. v380 清空锁文件实测：将“删除 phone_idle_lock”改成“空文本覆盖保存到 phone_idle_lock.txt”后，自动化到点能跳小手机，锁文件保留但显示 0KB，说明清锁成功且不再需要删除文件。该次测试角色未回复，下一步应查看小手机“久未打开调试”确认 phone_idle 是否收到、时间判断是否被“刚打开过”拦住，或 AI/生成链路是否异常。
25. v381 代码修复：手机实测显示收到 phone_idle 时“上次打开小手机”和事件时间同一分钟，导致时间判断为“未触发：离开 0分钟”。原因是打开小手机本身刷新了最后打开时间。v381 已在 idleTouchOpen() 刷新前保存上一轮打开时间，并在 handleIdleEvent() 遇到“当前打开刚刷新导致不足阈值”时，用打开前的时间补判；调试面板新增“判断用的时间”。

---

## 下一步应该做什么

小手机本体链路已经验证通过。

下一步只做一件事：

解决“多个 App 触发时只认第一次，不产生多个并行倒计时”。

要求：

1. 不再重复测试小手机内部模拟按钮，除非后续改动影响了它。
2. 不再走抖音方案。
3. 直达方案已有新证据证明不可行：hash、query、本地桥页面在手机主屏 webapp 中都没有稳定把事件传进去。
4. 下一步改为“云端事件收件箱 + 打开主屏小手机”：快捷指令先写入 phone_external_events，再打开主屏小手机，由小手机自己轮询云端事件并触发 phone_idle。
5. 每次只让用户做一个明确操作，并说明这一步和昨天已失败步骤的区别。
6. hash 和 query 都已排除，下一步不要再靠 URL 参数传事件。
7. 不再重复测试“专用本地桥页面/路径触发”，因为手机实测已经失败。
8. 云端事件收件箱链路已成功，不再重复调试 POST/打开小手机链路，除非后续改动影响它。
9. 同一个自动化里多选多个 App 已排除，不要原样重测。
10. 下一步要改的是快捷指令锁结构：让第一轮触发后立刻落锁，后续触发只停止，不进入等待；并避免删除文件弹窗/并发清锁。

---

## AI 必须遵守的工作规则

1. 每提出一步操作前，必须检查这一步是不是已经试过。
2. 如果试过，必须说明这次和上次有什么本质不同。
3. 如果没有新变量，禁止让我重复测试。
4. 每完成一次测试，必须更新“已验证/已排除清单”。
5. 不要只说“你说得对”。如果同意，必须说明这会如何改变后续计划。
6. 不要一次给我一大堆方案。一次只给我一个明确操作。
7. 我不是工程师，步骤必须具体到“点哪里、看什么、拍什么截图”。
8. 不要让我反复操作 GitHub、Supabase、Pages、Push，除非有明确新证据。
9. 如果你不确定，先问我拿截图，不要猜。
10. 当前目标完成前，不要扩展别的新功能。

---

## 当前判断

小手机内部问题已经基本排除。

现在问题范围缩小为：

iPhone 快捷指令云端事件链路已经成功；单 App 1 分钟自动化闭环成功；同一个自动化里多选多个 App 已失败。当前问题只剩：多个 App 触发时的并发倒计时。下一步必须先改锁结构或自动化拆分方式，不能原样重复多 App 测试。
