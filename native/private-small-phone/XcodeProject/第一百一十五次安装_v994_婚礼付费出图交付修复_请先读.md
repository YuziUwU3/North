# 第一百一十五次安装：v994 婚礼付费出图交付修复

- 网页资源：v994
- 私人 iOS：1.0.115 (115)
- 原生桥：24
- 交付日期：2026-08-19

## 本次必须确认

1. 完整解压并打开全新 `SmallPhone_v994_WeddingPaidImageDelivery` 工程，不能覆盖或继续编译旧 v969、v979、v990、v991、v992、v993 工程。
2. 设置页必须显示“当前已加载：v994 · 婚礼付费出图交付修复”和“私人安装包 1.0.115 (115)”。任一项不符都代表打开了旧包。
3. 婚礼每一幕只调用普通图片生成接口，不再先调用参考图聊天生图，也禁止图片接口失败后暗中切换聊天生图；单次请求允许平台在最多 5 分钟内返回，避免慢图刚完成就被客户端提前放弃。
4. 图片平台只要返回了可显示图片，就立刻缓存并用于婚礼；不再等待联网识图复核，也不会因为复核未通过而丢掉已经扣费返回的图片。
5. 整场后台准备只有在接口确实没有返回图片时才自动重试，最多两次；收到任何一张真实返回图后都不再为该幕自动追加付费请求。全部请求都没收到图片才使用内置预设图。
6. 单独重做某一章时，每点一次只发起一张图片请求，不会自动连发三次；原图在新图返回和保存前始终保留。
7. 第一幕人物身份改用角色外貌、固定礼服、发型与身份锚点生成稳定摘要并在同场复用，不再额外调用识图模型等待第一幕摘要。

## Mac 与真机验收边界

Windows 已完成源码语法、自动回归、网页／私人资源同步和包结构检查，但不能替代 Mac 编译、签名或真实 iPhone 验证。请在 Mac 首次打开后执行 `Product → Clean Build Folder`，使用 `PhoneCompanionTest` Scheme，核对外置平台每次单章点击只收到一条图片请求、真实图片能进入影集，并分别验证接口无图时的两次重试与预设兜底。
