# 第八十五次安装：v963 陪看字幕、共享画面与后台链路修复

- 网页核心：v963
- 私人 iOS：1.0.85 (85)
- 原生桥：21

本次依次完成四项：删除没有实际可用播放链路的优酷入口；修复私人 App 的 SRT／VTT 导入、UTF-16 字幕、非标准空行和画面 OCR 字幕写入；校正 ReplayKit 共享画面方向并裁出实际播放器区域，角色说话改走原生混音以免暂停 B站视频；后台主动消息在账号服务暂时异常时先验证既有安全绑定，不再每次核验都强制重新登录。首次绑定仍必须使用私人手机号账号。

安装时必须完整解压到全新目录 `SmallPhone_v963_CinemaSubtitleSharePush`，不要覆盖旧工程。用 Xcode 打开 `PhoneCompanionTest.xcodeproj`，执行 `Product → Clean Build Folder`，再选择 `PhoneCompanionTest` Scheme 和真实 iPhone 编译安装。设置页应显示 v963 与 1.0.85 (85)。Windows 自动测试不代表 Mac 编译、B站真机播放、ReplayKit 识别或真实 iPhone 后台通知已经完成。
