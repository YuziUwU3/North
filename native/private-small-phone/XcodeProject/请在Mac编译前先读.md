# 私人「小手机」Xcode 工程

## v936／1.0.60：必须从本次全新目录打开

- 本次压缩包使用全新顶层目录 `SmallPhone_v936_InstallVerified`。请完整解压到新目录，不能覆盖旧 v935 或更早目录后继续编译。
- 打开设置页最上方必须显示“当前已加载：v936 · 音乐完整导入与长单曲导出修复”和“私人安装包 1.0.60 (60)”。若不是这两行，说明打开或安装的仍是旧工程。
- 首次打开新工程后执行 `Product → Clean Build Folder`，再用 `PhoneCompanionTest` Scheme 真机运行；不需要删除仓库或其他旧压缩包。
- 本次包内 `PhoneWeb.bundle` 已直接同步 v936。音乐文件写入后会核对真实大小和首尾内容，文件缺失时可在原歌曲上重新选择文件，不删除原歌词和时间轴。
- 3–4 分钟或更大的本地单曲改用二进制 `.northmusic` 包导出，不再经过 Base64，也不再受旧版单曲 32 MB 上限影响；小型旧 JSON 音乐包仍可导入。
- 微信来电默认使用用户指定 MP3，同时保留“从音乐库选择来电铃声”；所选歌曲失效或删除后自动回到默认 MP3。
- iPhone 主屏幕网页底部系统区域按用户要求保持 v934 原处理，本版不再修改底栏。

此目录来自 2026-08-11 用户提供的真实 `AppleProjects/PhoneCompanionTest` 工程副本。它保留主 App、Device Activity Report、Monitor、Shield 和通知服务五个 Target，并把主 App 显示名称改为「小手机」。

## v895／1.0.20：请从全新目录打开

- 本工程应位于压缩包的 `SmallPhone_v895_FullReadMemoryPushFix` 全新目录中。不要把该目录覆盖或拖进旧 `XcodeProject`，否则 Xcode 可能继续编译旧网页核心或旧 Swift 文件。
- `registerPushTokenIfAvailable` 已是同 Target 内可访问；`ContentView` 已补齐 `approvedWithDataAccess`；通知扩展已显式链接 `Intents` 和 `UserNotifications`。
- 首次打开后执行 `Product → Clean Build Folder`，再选择 `PhoneCompanionTest` Scheme 和真机运行。如果仍有 `Command Ld failed`，必须展开并保留下一层具体链接器正文。

## 重要边界

- 当前审核中的公开 North 工程和已提交版本没有被修改。
- 私人副本沿用真实工程已经获权的 Team、Bundle ID、App Group 和 entitlements，因此安装到本人设备时会替换同 Bundle ID 的旧 North/伴生开发包，二者不能同时安装或控制。
- `PhoneWeb.bundle` 是从仓库共享网页核心生成的安装包资源，不是第二套业务源码。
- Windows 不能编译此工程。必须在 Mac 用当前 Xcode 编译全部五个 Target 并做真机测试。

## 本轮已合入

- 完整小手机网页作为本地资源运行，不以远程网页作为首页。
- 私人版本为 1.0.20 (20)；共享网页核心为 v895。当前审核中的公开 North App 工程仍未改动。
- 私人 App 已增加“手机号 + 密码”登录、Keychain 登录态以及按账号隔离的云备份／恢复；首次登录不会静默覆盖本机数据。中国大陆私人号码不再依赖短信验证码：不要启用 Phone Provider，也不需要配置 Twilio。编译前只需执行 `supabase/migrations/202608110001_private_phone_accounts.sql`，并在 Authentication 中手动建立已确认用户；邮箱按 `smallphone.86手机号@example.com` 生成，App 界面仍输入原手机号和密码。
- 原生设备管理只在「设置」页右上角打开，不再覆盖所有内置页面。
- 话筒转字幕走 iOS Speech/AVAudioEngine 原生桥；连续识别停顿 1.15 秒后会提交为最终用户发言，写入通话记录并触发角色回复。首次使用必须允许麦克风和语音识别权限。
- 通话与音乐聊天输入栏使用 WKWebView/iOS 自己的键盘避让；已移除原生键盘 frame、visualViewport 和网页固定偏移的重复补偿。
- 网页端苹果主屏幕适配保留稳定的 100% 外壳，并为可能返回 0 的安全区提供顶部 47px、底部 34px 兜底；不会套用到私人原生 App。
- 私人通知扩展会移除共享推送的固定假角标，App 启动和回前台也会清理旧红点。
- v881 已建立永久锁定账本、当日快照字段、前台授权复检和真实状态措辞；v894 继续保留这些逻辑。1.0.17 建立本机 `device.snapshot` 与 `device.command` 直连；1.0.18 新增 `readSessionId`、逐项真实读取、读取超时、ECG 摘要、明确手动解锁事件和统一 App 的 APNs 自登记；1.0.19 补齐角色对屏幕、App、步数、睡眠、心率、心电、HRV、电量和位置的查看触发，并在没有本次读取编号时拦截“没刷新/手表没戴”等假读取。手动一键读取也显示同一套逐项进度。Mac 编译前必须应用迁移 003/004并部署 phone-role-push；共同生活仍在会话停止后随机静默 30–60 分钟，间隔 1 分钟不能覆盖该硬静默。

## v895／1.0.20：第二十次安装补充

- 请只从新交付目录 `SmallPhone_v895_FullReadMemoryPushFix` 打开工程，不要覆盖回旧工程后继续编译。
- 1.0.20 在明确健康读取时真正打开 HealthKit 同步；屏幕时间在普通 `.approved` 授权下会挂载 `DeviceActivityReport`，由 Report 扩展经 App Group 回传本次总时长和逐 App 数据。`.approvedWithDataAccess` 仍走 iOS 26 直接接口。
- 电量和位置原本可读是正常的独立链路现象，不代表 HealthKit 或 Screen Time 已接通。验收必须逐项查看电量、总屏幕、逐 App、步数、睡眠、心率、心电、HRV 和位置。
- Supabase 迁移 003、004、005、006 与 `phone-role-push` 已在关联项目部署；005 专门兼容旧版 `p_apns_env` 参数，006 用于彻底清空服务端主动联系记忆。
- Windows 只能做静态和自动化核验；仍须在 Mac 编译五个 Target，并在真实 iPhone 上验证 Report 回传、HealthKit 授权、后台／强退通知、头像和主动来电。
- 真实 8 秒使用量读取超时；超时后不等待卡住的读取，并禁止晚到结果覆盖新快照。
- scene 进入 inactive 时卸载 SwiftUI Map，避免后台切换时 VectorKit 主线程看门狗；定位记录不随地图卸载而停止。

## v896／1.0.21：第二十一次安装补充

- 请只从新交付目录 `SmallPhone_v896_AllReadCompletionPushSchemaFix` 打开工程，不要覆盖回旧工程后继续编译。
- 用户要求“读取全部”后，普通角色回复会被压住；原生端完成所有请求读取器后才返回 `readFinishedAt`、`readComplete` 和 `readOutcomes`，角色随后固定逐项报告九类结果。
- 后台核验缺少 `controller_user_id` 的服务器前置迁移 002 已实际应用，迁移历史 002–006 已重新核对。这一服务器修复即时生效；1.0.21 安装包用于全量读取完成回执修复。
- Windows 475 项自动化测试通过。Mac 仍须编译五个 Target，并在真机只发一次“读取全部”确认顶部逐项进度结束后才收到九类汇总；同时验证 HealthKit、Report 回传、前后台／强退通知、头像和主动来电。

## v897／1.0.22：第二十二次安装补充

- 请只从新交付目录 `SmallPhone_v897_NaturalSingleReadReply` 打开工程，不要覆盖回旧工程后继续编译。
- v896 的原生读取本身能够返回数据，但网页在完成后强制把自然回复替换为逐项系统清单；同时聊天／通话识别读取意图过晚，普通模型可能先回复一轮。1.0.22 已删除这个硬覆盖，并把读取入口提前到普通模型之前。
- 读取期间只能看到顶部逐项进度，角色不能说“没读到”或先回答；同一轮全部完成后只允许一轮自然角色回复。未返回项目和权限／超时诊断保留在内部，不显示成角色气泡。
- Windows 475 项自动化测试通过。Mac 仍须编译五个 Target，并在真机只发一次“读取全部”，确认顶部进度完全结束后只收到一轮自然回复；同时验证 HealthKit、Report 回传、前后台／强退通知、头像和主动来电。

## v898／1.0.23：第二十三次安装补充

- 请只从新交付目录 `SmallPhone_v898_AllChannelInspectionGate` 打开工程，不要把文件逐个覆盖回旧工程后继续编译。
- v897 只按“同一条用户消息”去重，并且“查一次所有的”没有被解析成全部数据；普通模型因此可能先生成，后置守卫再启动真实读取，形成读取前后两轮回复。
- 1.0.23 改成聊天、电话和共同生活共用全局读取锁与生成代次。读取一排队就冻结普通输出；即使旧回复已经生成，只要读取代次发生变化，也会在真正发送前永久作废。
- 同一轮原生读取全部完成后，成功事实才进入隐藏上下文，角色只自然回复一次。电话的完成回复说完前不会释放读取通道，系统清单、权限诊断和超时原因不会显示成角色气泡。
- Windows 476 项自动化测试全部通过。Mac 仍须编译五个 Target，并在三个场景分别测试一次：微信聊天、电话、共同生活都应先完整显示顶部逐项读取，期间零角色回复，结束后只收到一轮自然回复。

## v899／1.0.24：第二十四次安装补充

- 请只从新交付目录 `SmallPhone_v899_ModelOnlyInspectionAndOfflineDiagnostics` 打开工程。
- 读取门禁造成的正常静默不再被当成模型空回复，因此不会再弹“模型没有生成／再点一次”；顶部逐项读取横幅保持不变。
- 读取完成后的话术只由角色模型生成，角色自行决定一条或多条；固定逐项汇总模板已删除。若模型两次修复仍不安全，本轮不发送虚假或系统式内容。
- 线下回复失败会显示余额／额度、密钥、权限、限流、接口或模型配置、网络超时、空输出等真实类别，且明确保留原对话。
- Windows 478 项自动化测试全部通过。Mac 仍须编译五个 Target，并分别验收微信聊天、电话、共同生活以及线下回复失败提示。

## 尚未完成

- Mac 编译和签名验证。
- 安装前的数据迁移与恢复入口。
- 手机号账号仍需在 Supabase 创建首名私人用户；完整大容量原生媒体存储仍未完成。
- 真机锁定、解锁、使用时间、前后台 20 次、断网、跨日和长时间稳定性测试。
- 远端迁移 003–006 与 `phone-role-push` 已部署；APNs 凭据、设备令牌和真实后台送达仍需真机核验。
# v929 / 第五十四次安装补充

- 当前私人版版本号：1.0.57 (57)，内置网页核心：v933，原生桥契约：18。

# v931 / 第五十五次安装补充

- 本次同时更新网页版与私人 App，共享网页核心为 v931，私人版本为 1.0.55 (55)。
- 微信模型无可见回复会说明真实类别；输入光标不再依赖视觉视口强制重绘；安卓首屏拖动在落点提交前会完成最终位置校验，拒绝保存丢失或重复应用的布局。
- 网页不再暴露屏幕共享入口；私人 App 继续保留 ReplayKit、原生桥、摄像头及通话音频能力。
- 清水玻璃屏保的时间数字使用白色线条高光；同一角色连续通知必须是一条一张独立玻璃卡，不能由黑色阴影连接。
- 锁定软件的原图标必须保持原色和原尺寸，只显示名称右侧红锁。
- 网页苹果底部安全区修复不会改变私人 App 的原生安全区布局。
- 编译后请在真机分别检查：黑、灰白、粉白、蓝白四个玻璃主题的锁定图标；再检查微信底栏、首屏拖拽、通话声音和字幕没有回归。

# v932 / 第五十六次安装补充

- 本次同时更新网页版与私人 App，共享网页核心为 v932，私人版本为 1.0.56 (56)，原生桥契约仍为 18。
- iOS 新版主屏幕 Web App 的底部额外区域由系统绘制，不属于网页视口。v932 不再把页面强行延伸到该区域；网页锁屏的手电筒、相机和上滑提示恢复在可见视口内，私人 App 的底部按钮布局不改。
- 网页会按当前主页壁纸底部颜色设置系统栏背景。第一次打开 v932 后，请把主屏幕 Web App 从后台完全划掉再重新打开一次，让 iOS 刷新其缓存颜色；不需要重新添加到主屏幕。

# v933 / 第五十七次安装补充

- 本次同时更新网页版与私人 App，共享网页核心为 v933，私人版本为 1.0.57 (57)，原生桥契约仍为 18。
- “回到屏保”小箭头只在主页第一页显示；切换到第二页后立即隐藏。网页版苹果主屏幕且开启“苹果兼容适配”时，玻璃主页内容单独下移给箭头留位；Android 和私人 App 的主页主体不套用这条位移。
- 微信底部输入栏只调整输入区域：“让 ta 回”按钮保持原样，输入栏上提 4px，左右间距收窄以放宽输入框；文字和光标在原 36px 输入框内上移并增加下方余量。
- 双人头像组件沿用情侣空间开始日期，只在中间显示“相恋 X 天”，与角色名字使用同一文字样式，不加独立框、气泡或阴影。
- 私人 App 屏幕共享通话新增“逐句看”：开启后每个完成句只读取一次当时画面，并暂停定时/实时额外扫描；关闭后恢复原来的共享识别逻辑。
- 偏好设置的来电铃声新增“从音乐库选择歌曲”；在线收藏与本地导入歌曲都可作为循环来电铃声，删除所选歌曲或文件失效时自动恢复默认轻响。
- 键盘仍只由 WKWebView / iOS 自己避让。禁止恢复原生 keyboard frame、`visualViewport` 差值、强制滚动或强制重画选区的重复补偿。
- 苹果兼容开关继续保留。它只在苹果主屏幕 Web App 或私人 iOS App 内抬高锁屏入口小箭头；Android 不套用这些类，原位置和点击范围保持不变。
- 黑、灰白、粉白、蓝白四套主题的 96 个应用图标会加入离线缓存，图片失败时保留线条或 emoji 占位，不再出现只剩应用名的空位。
- Windows 无法编译和签名 iOS 工程；拿到安装包后仍需在 Mac 用 Xcode 编译五个 Target，并在真实 iPhone 上验收主屏幕系统栏、小箭头、锁屏按钮与图标恢复。
