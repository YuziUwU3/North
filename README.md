# 小手机

当前基线：网页核心 v900／私人 iOS 1.0.24 (24)。主聊天与辅助模型现在会在保存、测试和真正请求前识别语音合成专用模型；`speech-*`、`tts-*` 及名称中独立的 `-tts` 标记不能再误走 `/chat/completions`，会明确提示改填到“语音模型 / TTS”。聊天与语音原有独立路线保持不变，旧存档中已经误填的模型也会在启用或调用时被安全拦截。v899 的真实读取锁、角色自主回复和线下错误诊断全部保留；本轮未修改私人 iOS 原生代码。维护与未完成真机验收边界见 `docs/maintenance/私人小手机App_唯一总纲.md`。
