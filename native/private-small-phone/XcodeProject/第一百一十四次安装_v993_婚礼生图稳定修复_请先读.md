# 第一百一十四次安装：v993 婚礼生图稳定修复

- 网页资源：v993
- 私人 iOS：1.0.114 (114)
- 原生桥：24
- 交付日期：2026-08-19

## 本次必须确认

1. 完整解压并打开全新 `SmallPhone_v993_WeddingGenerationStability` 工程，不能覆盖或继续编译旧 v969、v979、v990、v991、v992 工程。
2. 设置页必须显示“当前已加载：v993 · 婚礼生图稳定修复”和“私人安装包 1.0.114 (114)”。任一项不符都代表打开了旧包。
3. 单章重做和整场生成仍优先参考第一幕人物；若外置平台的参考图聊天线路在 60 秒内失败，会立刻恢复普通生图兜底，并在同一轮后续章节跳过该坏线路，不再连续三次长时间空等。
4. 第一幕人物特征只识别一次并在同一轮复用；普通生图兜底仍携带脸型、五官、发型、发色、发冠和礼服身份摘要，生成后继续执行动作与人物复核。
5. 情侣空间的六张婚礼图可点开高清大图；大图底部有明确的“保存高清原图”按钮，同时保留长按保存。
6. 影集顶部和线下约会的已完成婚礼卡都要显示“单独重做某一章”。成功后只覆盖所选章节，其他五张、婚书和记忆不变。
7. 即使用户删除了角色发来的就绪邀请卡，线下约会也必须从最新完整素材恢复并显示“立即进入婚礼”，不能永远卡在“等待十秒后进入”。

## Mac 与真机验收边界

Windows 已进行源码语法、自动回归、网页／私人资源同步和包结构检查，但不能替代 Mac 编译、签名或真实 iPhone 验证。请在 Mac 首次打开后执行 `Product → Clean Build Folder`，使用 `PhoneCompanionTest` Scheme 验证参考图线路失败后的普通生图兜底、Photos 权限、大图保存、App 后台恢复、单章重做与实际生图缓存。
