# 第九十六次安装：v974 线下记忆与授权容灾修复

- 网页核心：v974
- 私人 iOS：1.0.96 (96)
- 原生桥：23

请完整解压到全新目录 `SmallPhone_v974_OfflineMemoryLicenseFailover`，不要覆盖任何旧工程。用 Xcode 打开 `PhoneCompanionTest.xcodeproj`，先执行 `Product → Clean Build Folder`，再选择 `PhoneCompanionTest` Scheme 和真实 iPhone 编译安装。设置页应显示“v974 · 线下记忆与授权容灾修复”和“1.0.96 (96)”。

本版让线下约会、共同生活和微信共用同一套重要记忆。明确要求“记住／记到微信记忆”会确定性保存，模型主动记忆标签只执行一次且不会显示；登录微信、查看手机、屏幕共享、远程控制、同步、读取、上传和报错等操作不会写进重要记忆。

伴生真实同步遇到超时、408、429、5xx 或 520–524 时会短暂重试一次；仍失败时保留最近一次有效真实数据，只显示简短中文原因，不再把 Cloudflare 错误页 HTML 当成数据。邀请码授权新增独立主备路由：旧项目优先，故障时切备用项目，并记住最近成功入口。AI、好友、真实同步等其他后台没有被切换，旧项目也没有被删除或覆盖。

Windows 自动测试只能验证网页、桥接协议与工程结构，不能替代 Mac 上的 Xcode 编译、签名和真实 iPhone 验收。
