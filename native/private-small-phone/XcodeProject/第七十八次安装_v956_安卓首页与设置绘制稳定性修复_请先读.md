# 第七十八次安装：v956 安卓首页与设置绘制稳定性修复

当前候选版本：

- 网页核心：v956
- 私人 iOS：1.0.78 (78)
- 原生桥：18

本次只处理安卓触屏设备的两个绘制稳定性问题：

1. 首页三个大组件的数据、布局和拖动逻辑保持不变；触屏设备关闭组件自身的大面积实时背景模糊，继续保留渐变、边框、阴影和全部内容，避免页面恢复或显存紧张后整块不绘制。
2. 设置页的区块、按钮和输入框在触屏设备关闭逐层实时背景模糊，保留原样式和全部配置、开关、保存、分页功能，避免滚动卡顿及局部画面不刷新。
3. 桌面浏览器仍保留完整毛玻璃效果；未修改微信、通话、屏幕共享、音乐、锁定、限额、角色行为或云端数据逻辑。

Windows 已完成 662/662 自动测试和安卓／iPhone／桌面三环境模拟绘制核对。这里没有 Mac，不能把该结果写成 Xcode 编译、签名或真实 iPhone 通过。

Mac 与真机验收顺序：

1. 从全新目录 `SmallPhone_v956_AndroidTouchPaintFix` 打开工程，全 Target 编译 1.0.78 (78)，不要从旧 ZIP 覆盖 `PhoneWeb.bundle`。
2. 设置页顶部确认显示 v956 和私人安装包 1.0.78 (78)。
3. 在安卓浏览器打开首页，反复切后台、回前台、锁屏再恢复，确认信息条、唱片和双人组件持续显示，应用、Dock 和拖动位置不变。
4. 在安卓设置页连续滚动三个分页、输入和保存 API 配置，确认不再明显卡顿或出现局部空白。
5. 在 Mac／iPhone 核对桌面毛玻璃、微信、电话、音乐、屏幕共享、锁定与每日限额仍保持原行为。

若任一步失败，保留截图、设备型号、系统版本、浏览器名称和准确操作顺序，不要同时改变多个变量。
